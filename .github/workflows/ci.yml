name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'  # keep in sync with .devcontainer/Dockerfile
          cache: true

      - name: Build
        run: go build ./...

      - name: Test
        run: go test ./...

      - name: Verify generated files are up to date
        run: |
          go generate ./boardtype/...
          git diff --exit-code
